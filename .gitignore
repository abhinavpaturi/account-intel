# Generated dossiers are private prospect research — keep them local.
# Committed sample(s) under reports/samples/ are the exception (see negation below).
reports/*
!reports/samples/
!reports/.gitkeep

# OS / editor noise
.DS_Store
*.swp
.idea/
.vscode/

# Never commit secrets or local env
.env
.env.*
